{% extends 'base.html.twig' %}

{% block title %}ImageProject index{% endblock %}

{% block body %}
    <h1>ImageProject index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for image_project in image_projects %}
            <tr>
                <td>{{ image_project.id }}</td>
                <td>{{ image_project.name }}</td>
                <td>
                    <a href="{{ path('image_project_show', {'id': image_project.id}) }}">show</a>
                    <a href="{{ path('image_project_edit', {'id': image_project.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('image_project_new') }}">Create new</a>
{% endblock %}