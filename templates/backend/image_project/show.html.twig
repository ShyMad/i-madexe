{% extends 'base.html.twig' %}

{% block title %}ImageProject{% endblock %}

{% block body %}
    <h1>ImageProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ image_project.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('image_project_index') }}">back to list</a>

    <a href="{{ path('image_project_edit', {'id': image_project.id}) }}">edit</a>

    {{ include('image_project/_delete_form.html.twig') }}
{% endblock %}