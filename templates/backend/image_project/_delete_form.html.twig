<form method="post" action="{{ path('image_project_delete', {'id': image_project.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_project.id) }}">
    <button class="btn">Delete</button>
</form>