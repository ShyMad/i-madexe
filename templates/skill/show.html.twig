{% extends 'base.html.twig' %}

{% block title %}Skill{% endblock %}

{% block body %}
    <h1>Skill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ skill.title }}</td>
            </tr>
            <tr>
                <th>Lvl</th>
                <td>{{ skill.lvl }}</td>
            </tr>
            <tr>
                <th>TypeOfSkill</th>
                <td>{{ skill.typeOfSkill }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('skill_index') }}">back to list</a>

    <a href="{{ path('skill_edit', {'id': skill.id}) }}">edit</a>

    {{ include('skill/_delete_form.html.twig') }}
{% endblock %}